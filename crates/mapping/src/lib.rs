//! The mapping graph IR: nodes, connections, functions, and conditions that
//! describe how a source schema is transformed into a target schema, plus
//! (de)serialization of ferrule project files.
